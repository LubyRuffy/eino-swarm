# Pin the public registry so a user-level mirror cannot rewrite
# package-lock.json. npm 12 refuses lockfile tarballs whose host is
# not the install registry (EALLOWREMOTE) and a fresh clone dies.
registry=https://registry.npmjs.org/
